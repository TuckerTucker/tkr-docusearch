# Frontend Environment Variables
# Copy this file to .env and configure as needed
# Note: Only variables prefixed with VITE_ are exposed to the client

# API Configuration
# Leave empty to use Vite proxy (recommended for development)
# The proxy routes requests to the appropriate backend services:
#   /api/research/* -> http://localhost:8004 (Research API)
#   /documents/*    -> http://localhost:8002 (Worker API)
#   /uploads/*      -> http://localhost:8000 (Copyparty)
#
# For production, set to your API server URL:
#   VITE_API_URL=https://your-api-domain.com
VITE_API_URL=

# Copyparty Upload Credentials
# WARNING: These credentials are exposed to the browser client
# Use a dedicated upload-only account with limited permissions
VITE_UPLOAD_USERNAME=uploader
VITE_UPLOAD_PASSWORD=docusearch2024
