# DocuSearch MVP Requirements - Wave 3
# Python 3.10+

# Core ML/AI dependencies
torch>=2.8.0  # CVE patch: updated from 2.0.0
torchvision>=0.15.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# ColPali - Multimodal embedding model
# Install from GitHub: pip install git+https://github.com/illuin-tech/colpali.git
colpali-engine>=0.2.0

# Image processing
Pillow>=10.0.0

# Document parsing - unified with Docling
# Docling handles PDF, DOCX, PPTX, and other formats
# [asr] extra includes Whisper support for MP3/WAV audio transcription
docling[asr]>=2.55.0

# Vector database
chromadb>=0.4.0

# Data and utilities
numpy>=1.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0
tqdm>=4.65.0
structlog>=23.1.0
urllib3>=2.5.0  # CVE patch: explicit version pinning

# Audio metadata extraction (ID3 tags, album art)
mutagen>=1.47.0

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# File watching
watchdog>=3.0.0

# LLM Integration
litellm>=1.0.0
tiktoken>=0.5.0  # Token counting for OpenAI models
mlx-lm>=0.26.3  # Local LLM preprocessing with Metal GPU acceleration

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
